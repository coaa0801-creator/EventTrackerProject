import { JobAssignedPipe } from './job-assigned.pipe';

describe('JobAssignedPipe', () => {
  it('create an instance', () => {
    const pipe = new JobAssignedPipe();
    expect(pipe).toBeTruthy();
  });
});
